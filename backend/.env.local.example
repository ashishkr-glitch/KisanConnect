# Example backend environment file for local development
# Copy to `.env.local` and fill in real values. DO NOT commit `.env.local`.
# Backend reads GEMINI_API_URL and GEMINI_API_KEY from environment.

# If you want the backend to proxy requests to the Gemini-compatible API,
# set GEMINI_API_URL to the external endpoint, and GEMINI_API_KEY to the secret.

GEMINI_API_URL=https://your-gemini-endpoint.example/v1/generate
GEMINI_API_KEY=PLACEHOLDER_DO_NOT_COMMIT

# Other useful env vars (optional)
# SPRING_PROFILES_ACTIVE=dev