# Local environment example for backend (do NOT commit real keys)
# Copy this file to .env.local or set env vars in your shell before starting the backend.

# Gemini / Margdarshak
# Recommended model (likely to have free-tier access for testing): `gemini-flash-lite-latest`
# If this model still returns 429, check Cloud Console quotas or use a different model from your project.
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-lite-latest:generateContent
GEMINI_API_KEY=<YOUR_GEMINI_API_KEY>

# Database (example values)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/Kisan2
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=changeme

# Server port (optional override)
SERVER_PORT=8081
